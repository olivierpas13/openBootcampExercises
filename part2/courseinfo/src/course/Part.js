export const Part = (parts) => {
  const partes = parts.course;
  console.log(partes);
};
//   return (
//     {parts.map(curso) =>{
//       <p key={parts.id}>{parts.name}</p>
//     }}
//   );
// }

//     {course.parts.map((curso) => (
//     <p key={curso.id}>{curso.name}</p>
//   ))}
// };
